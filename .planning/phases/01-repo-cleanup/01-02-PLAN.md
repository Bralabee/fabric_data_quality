---
phase: 01-repo-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - PKG-02
  - PKG-03

must_haves:
  truths:
    - "No build artifacts (build/, dist/, htmlcov/, .coverage, pipeline.log, egg-info) exist in the working tree"
    - ".gitignore prevents re-committing all known artifact patterns"
  artifacts:
    - path: ".gitignore"
      provides: "Coverage for all build artifact patterns"
      contains: "build/"
  key_links:
    - from: ".gitignore"
      to: "build/ dist/ htmlcov/ .coverage pipeline.log *.egg-info/"
      via: "gitignore pattern matching"
      pattern: "build/|dist/|htmlcov/|\\.coverage|\\*.log|\\*.egg-info"
---

<objective>
Clean local build artifacts from the working tree and verify .gitignore coverage prevents re-committing them.

Purpose: Ensure the repository working tree has no stale build artifacts and that .gitignore patterns prevent them from being accidentally committed in the future.

Output: Clean working tree with no build artifacts, verified .gitignore coverage.
</objective>

<execution_context>
@/home/sanmi/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-cleanup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean local build artifacts and verify .gitignore</name>
  <files>.gitignore</files>
  <action>
1. **Clean local build artifacts** (these are untracked files, NOT in git):
   ```
   rm -rf build/ dist/ htmlcov/ fabric_data_quality.egg-info/
   rm -f .coverage pipeline.log
   ```
   Verify each is gone after deletion.

2. **Verify .gitignore coverage** for all required patterns:
   Run `git check-ignore` for each pattern to confirm .gitignore blocks them:
   - `git check-ignore build/`
   - `git check-ignore dist/`
   - `git check-ignore htmlcov/`
   - `git check-ignore .coverage`
   - `git check-ignore pipeline.log`
   - `git check-ignore fabric_data_quality.egg-info/`

   Research confirms all patterns are already covered:
   - `build/` (line 7), `dist/` (line 9), `*.egg-info/` (line 19)
   - `.coverage` (line 38), `htmlcov/` (line 39), `*.log` (line 56)

3. **Review .gitignore for completeness** -- check if any additional artifact patterns should be added. Specifically verify these are covered:
   - `*.whl` files (wheel packages) -- check if `dist/` coverage is sufficient or if a direct `*.whl` pattern is needed for wheels outside dist/
   - `*.tar.gz` sdist files -- same consideration

   If `*.whl` or `*.tar.gz` are NOT covered by existing patterns, add them to the .gitignore under the Python section. Only add patterns that are genuinely missing -- do NOT add redundant entries.

4. **Final verification** -- create temporary test files to confirm gitignore works:
   ```
   mkdir -p build && touch build/test
   git check-ignore build/test && rm -rf build/
   ```
  </action>
  <verify>
    <automated>test ! -d build -a ! -d dist -a ! -d htmlcov -a ! -f .coverage -a ! -f pipeline.log -a ! -d fabric_data_quality.egg-info && git check-ignore build/ dist/ htmlcov/ .coverage pipeline.log fabric_data_quality.egg-info/ && echo "PASS"</automated>
  </verify>
  <done>No build artifacts exist in working tree. git check-ignore confirms .gitignore blocks all artifact patterns (build/, dist/, htmlcov/, .coverage, pipeline.log, *.egg-info/).</done>
</task>

</tasks>

<verification>
Phase-level verification for this plan:

1. `test ! -d build -a ! -d dist -a ! -d htmlcov -a ! -f .coverage -a ! -f pipeline.log -a ! -d fabric_data_quality.egg-info && echo "PASS"` -- no artifacts exist
2. `git check-ignore build/ dist/ htmlcov/ .coverage pipeline.log fabric_data_quality.egg-info/` -- all patterns blocked
3. `pytest tests/ -x --no-cov -q` -- no regression
</verification>

<success_criteria>
- No build artifacts (build/, dist/, htmlcov/, .coverage, pipeline.log, egg-info) exist in the working tree
- `git check-ignore` confirms all artifact patterns are blocked by .gitignore
- Existing test suite passes without regression
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-cleanup/01-02-SUMMARY.md`
</output>
