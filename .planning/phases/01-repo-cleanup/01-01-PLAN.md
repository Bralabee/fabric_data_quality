---
phase: 01-repo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - setup.py
  - docs/FABRIC_INTEGRATION.md
  - docs/FILE_STRUCTURE.md
  - docs/PROJECT_SUMMARY.md
  - README.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements:
  - PKG-01

must_haves:
  truths:
    - "setup.py does not exist in the repository"
    - "pip install -e . succeeds using only pyproject.toml"
    - "python -c 'import dq_framework' succeeds after editable install"
    - "No documentation references setup.py as a current file or build method"
  artifacts:
    - path: "setup.py"
      provides: "MUST NOT EXIST (deleted)"
  key_links:
    - from: "pyproject.toml"
      to: "pip install -e ."
      via: "[build-system] requires setuptools>=65.0"
      pattern: "build-system"
---

<objective>
Remove the legacy setup.py and update all documentation references to use pyproject.toml/PEP 517 build commands.

Purpose: Eliminate the conflicting packaging file (setup.py has v1.2.0 and GX <1.0 deps while pyproject.toml has v2.0.0 and GX >=1.0). After this plan, pyproject.toml is the single source of truth for packaging.

Output: setup.py deleted, all doc references updated, editable install verified working with pyproject.toml only.
</objective>

<execution_context>
@/home/sanmi/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-cleanup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove setup.py and verify editable install</name>
  <files>setup.py</files>
  <action>
1. First, verify that `pip install -e .` works WITHOUT setup.py by temporarily renaming it:
   - `mv setup.py setup.py.bak`
   - `pip install -e .`
   - `python -c "import dq_framework; print(dq_framework.__version__)"`
   - If this fails, restore setup.py and STOP -- document the failure.

2. If editable install succeeds, delete setup.py permanently:
   - `mv setup.py.bak setup.py` (restore first)
   - `git rm setup.py`

3. Verify again after git rm:
   - `pip install -e .`
   - `python -c "import dq_framework; print(dq_framework.__version__)"`

Note: setup.py has version 1.2.0 with `great-expectations>=0.18.0,<1.0.0` and `python_requires>=3.8`. pyproject.toml has version 2.0.0 with `great_expectations>=1.0.0` and `python_requires>=3.10`. The pyproject.toml is canonical.
  </action>
  <verify>
    <automated>test ! -f setup.py && pip install -e . 2>&1 | tail -1 && python -c "import dq_framework; print('OK:', dq_framework.__version__)"</automated>
  </verify>
  <done>setup.py does not exist, pip install -e . succeeds with pyproject.toml only, dq_framework imports successfully</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation references to setup.py</name>
  <files>docs/FABRIC_INTEGRATION.md, docs/FILE_STRUCTURE.md, docs/PROJECT_SUMMARY.md, README.md, .planning/codebase/STRUCTURE.md</files>
  <action>
Search all files for references to `setup.py` and update them:

1. **docs/FABRIC_INTEGRATION.md** (line ~24): Change `python setup.py bdist_wheel` to `python -m build --wheel`. Update any surrounding text that mentions setup.py as the build method.

2. **docs/FILE_STRUCTURE.md** (line ~10): Remove the `setup.py` entry from the file listing. Do NOT add a replacement line -- pyproject.toml is already listed.

3. **docs/PROJECT_SUMMARY.md** (line ~44): Remove the `setup.py` entry from the file listing.

4. **README.md** (line ~29): Remove the `setup.py` entry from any file structure listing.

5. **.planning/codebase/STRUCTURE.md** (line ~64): Remove the `setup.py    # Legacy setup (for editable installs)` line from the directory layout. This is lower priority but keeps planning docs accurate.

For each file, read it first, find the specific lines referencing setup.py, and make targeted edits. Do NOT rewrite entire files -- only change the lines that reference setup.py.

Also run a broad search to catch any other references: `grep -rn "setup.py" --include="*.md" --include="*.txt" --include="*.yml" --include="*.yaml" .` and update any additional hits (excluding .planning/phases/ research/context docs which are historical records).
  </action>
  <verify>
    <automated>grep -rn "setup\.py" --include="*.md" docs/ README.md .planning/codebase/ | grep -v "phases/" | grep -v "RESEARCH" | grep -v "CONCERNS" || echo "PASS: No setup.py references in docs"</automated>
  </verify>
  <done>No active documentation references setup.py as a current file or build method. Historical references in research/planning phase docs are acceptable.</done>
</task>

</tasks>

<verification>
Phase-level verification for this plan:

1. `test ! -f setup.py && echo "PASS: setup.py removed"` -- file is gone
2. `pip install -e . && python -c "import dq_framework"` -- editable install works
3. `grep -rn "setup.py" --include="*.md" docs/ README.md .planning/codebase/ | grep -v "phases/"` -- no doc references
4. `pytest tests/ -x --no-cov -q` -- existing tests still pass (no regression)
</verification>

<success_criteria>
- setup.py does not exist in the repository
- `pip install -e .` succeeds using only pyproject.toml
- `python -c "import dq_framework"` succeeds
- All user-facing documentation references updated build commands (python -m build)
- Existing test suite passes without regression
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-cleanup/01-01-SUMMARY.md`
</output>
