---
phase: 05-storage-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dq_framework/storage.py
  - tests/test_storage.py
  - tests/conftest.py
autonomous: true
requirements:
  - STOR-01
  - STOR-03

must_haves:
  truths:
    - "ResultStore ABC raises TypeError when instantiated with missing methods"
    - "JSONFileStore writes JSON files to a configurable local directory"
    - "JSONFileStore reads, lists, and deletes stored results"
    - "LakehouseStore delegates to mssparkutils.fs for all CRUD operations"
    - "get_store() returns JSONFileStore when not in Fabric runtime"
    - "get_store() returns LakehouseStore when in Fabric runtime"
    - "get_store() respects explicit backend override parameter"
  artifacts:
    - path: "dq_framework/storage.py"
      provides: "ResultStore ABC, JSONFileStore, LakehouseStore, get_store factory, make_result_key, _prepare_for_serialization"
      exports: ["ResultStore", "JSONFileStore", "LakehouseStore", "get_store", "make_result_key"]
    - path: "tests/test_storage.py"
      provides: "Unit tests for all storage classes and factory"
      min_lines: 120
  key_links:
    - from: "dq_framework/storage.py"
      to: "dq_framework/utils.py"
      via: "_is_fabric_runtime() and get_mssparkutils() imports in factory and LakehouseStore"
      pattern: "from \\.utils import"
    - from: "tests/test_storage.py"
      to: "dq_framework/storage.py"
      via: "direct import of all public classes"
      pattern: "from dq_framework\\.storage import"
---

<objective>
Create the ResultStore abstraction layer with JSONFileStore and LakehouseStore backends, plus the get_store() factory function and comprehensive tests.

Purpose: Establish the pluggable storage interface that replaces inline Lakehouse writes and enables local development result persistence. This module is the foundation for Phase 9 (Validation History) which will add SQLiteStore and ParquetStore backends.

Output: `dq_framework/storage.py` with ABC + two backends + factory, and `tests/test_storage.py` with full unit test coverage.
</objective>

<execution_context>
@/home/sanmi/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-storage-abstraction/05-RESEARCH.md

@dq_framework/utils.py
@dq_framework/constants.py
@tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From dq_framework/utils.py:
```python
def _is_fabric_runtime() -> bool:
    """Check if running in MS Fabric environment."""

def get_mssparkutils():
    """Get mssparkutils object, or None if not available."""

FABRIC_AVAILABLE: bool  # True if notebookutils is importable
FABRIC_UTILS_AVAILABLE: bool  # Alias for FABRIC_AVAILABLE
```

From tests/conftest.py (existing fixture to extend):
```python
@pytest.fixture
def mock_mssparkutils():
    """MagicMock for mssparkutils.fs operations (head, put, ls, mkdirs)."""
    utils = MagicMock(name="mssparkutils")
    utils.fs.head.return_value = ""
    utils.fs.put.return_value = None
    utils.fs.ls.return_value = []
    utils.fs.mkdirs.return_value = True
    return utils

@pytest.fixture
def sample_validation_result():
    """Returns a dict matching the standard validation result structure."""
    # See conftest.py lines 101-115 for full structure
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_storage.py with test scaffolds for all storage behaviors</name>
  <files>tests/test_storage.py, tests/conftest.py</files>
  <behavior>
    - ABC enforcement: Instantiating a subclass of ResultStore that omits any abstract method raises TypeError
    - JSONFileStore.write: Creates a .json file in results_dir with correct content
    - JSONFileStore.read: Returns dict from stored JSON file; raises FileNotFoundError for missing key
    - JSONFileStore.list: Returns sorted list of keys; filters by prefix when provided; returns empty list for empty dir
    - JSONFileStore.delete: Returns True and removes file for existing key; returns False for missing key
    - JSONFileStore.__init__: Creates results_dir if it does not exist
    - LakehouseStore.__init__: Raises RuntimeError when mssparkutils unavailable
    - LakehouseStore.write: Calls mssparkutils.fs.put with correct path and serialized data
    - LakehouseStore.read: Calls mssparkutils.fs.head with 10MB maxBytes limit
    - LakehouseStore.list: Returns sorted keys from mssparkutils.fs.ls; handles prefix filter; returns empty list on error
    - LakehouseStore.delete: Calls mssparkutils.fs.rm; returns True on success, False on error
    - get_store(backend=None): Returns JSONFileStore when _is_fabric_runtime() is False
    - get_store(backend=None): Returns LakehouseStore when _is_fabric_runtime() is True
    - get_store(backend="local"): Always returns JSONFileStore
    - get_store(backend="fabric"): Returns LakehouseStore when in Fabric; raises RuntimeError when not
    - get_store(backend="invalid"): Raises ValueError
    - make_result_key: Produces "validation_{safe_name}_{timestamp}" format
    - _prepare_for_serialization: Strips "validation_result" key, serializes with default=str
  </behavior>
  <action>
Create `tests/test_storage.py` with pytest-native test classes following project conventions (see TESTING.md patterns). Use `tmp_path` fixture for JSONFileStore tests. Use `unittest.mock.patch` and `MagicMock` for LakehouseStore and get_store tests.

Test class structure:
- `TestResultStoreABC` -- ABC enforcement test
- `TestJSONFileStore` -- CRUD operations using `tmp_path`
- `TestLakehouseStore` -- CRUD operations with mocked mssparkutils
- `TestGetStore` -- Factory function with patched `_is_fabric_runtime`
- `TestMakeResultKey` -- Key format validation
- `TestPrepareForSerialization` -- Serialization helper

For LakehouseStore tests, mock `dq_framework.storage.get_mssparkutils` to return a MagicMock, and mock `dq_framework.storage.FABRIC_AVAILABLE` to True. Use the existing `mock_mssparkutils` fixture pattern from conftest.py.

Also update `tests/conftest.py` to add `fs.rm` and `fs.exists` return values to the existing `mock_mssparkutils` fixture:
```python
utils.fs.rm.return_value = None
utils.fs.exists.return_value = True
```

All tests should initially fail (RED phase) because `dq_framework/storage.py` does not exist yet. Write the imports to reference the module that Task 2 will create.
  </action>
  <verify>
    <automated>cd /mnt/Storage/Insync/olusanmi_18th@hotmail.com/OneDrive/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY && python -c "import ast; ast.parse(open('tests/test_storage.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>tests/test_storage.py exists with 18+ test functions covering all behaviors listed above. conftest.py mock_mssparkutils fixture updated with fs.rm and fs.exists. All test files parse without syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dq_framework/storage.py with ResultStore ABC, JSONFileStore, LakehouseStore, and get_store factory</name>
  <files>dq_framework/storage.py</files>
  <behavior>
    - ResultStore ABC with write/read/list/delete abstract methods
    - JSONFileStore creates directory on init, implements all 4 CRUD methods with pathlib
    - LakehouseStore lazy-imports mssparkutils via get_mssparkutils(), implements all 4 CRUD methods
    - get_store() auto-selects backend via _is_fabric_runtime()
    - make_result_key() generates sortable keys
    - _prepare_for_serialization() strips validation_result key, uses default=str
  </behavior>
  <action>
Create `dq_framework/storage.py` following the research document's architecture patterns exactly. The research doc (05-RESEARCH.md) has complete implementation examples for all classes -- follow those closely.

Key implementation details:
1. **ResultStore ABC**: 4 abstract methods (write, read, list, delete) with type hints using `dict[str, Any]`, `Optional[str]`, `list[str]`, `bool`. Use Google-style docstrings.

2. **_prepare_for_serialization(data)**: Filter out "validation_result" key (GX object), serialize with `json.dumps(data, indent=2, default=str)`. Return string.

3. **make_result_key(batch_name)**: Sanitize batch_name (replace non-alphanumeric with underscore), append `datetime.now().strftime("%Y%m%d_%H%M%S")`. Return `f"validation_{safe_name}_{timestamp}"`.

4. **JSONFileStore(results_dir="dq_results")**: `__init__` creates dir via `Path.mkdir(parents=True, exist_ok=True)`. write() uses `Path.write_text()`. read() uses `Path.read_text()` + `json.loads()`. list() uses `Path.glob("*.json")` returning stems. delete() uses `Path.unlink()`.

5. **LakehouseStore(results_dir="Files/dq_results")**: `__init__` imports via `from .utils import get_mssparkutils, FABRIC_AVAILABLE` -- raises RuntimeError if unavailable. write() uses `mssparkutils.fs.put(path, content, True)`. read() uses `mssparkutils.fs.head(path, 10_000_000)`. list() uses `mssparkutils.fs.ls()`. delete() uses `mssparkutils.fs.rm(path, False)`.

6. **get_store(results_dir=None, backend=None)**: Lazy import `_is_fabric_runtime` from `.utils`. Auto-select or honor explicit backend. Raise ValueError for unknown backend. Raise RuntimeError for backend="fabric" when not in Fabric.

Follow project conventions: `logger = logging.getLogger(__name__)`, f-string log messages, module docstring with usage example.
  </action>
  <verify>
    <automated>cd /mnt/Storage/Insync/olusanmi_18th@hotmail.com/OneDrive/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY && pytest tests/test_storage.py -x -v</automated>
  </verify>
  <done>All tests in test_storage.py pass (GREEN). dq_framework/storage.py exports ResultStore, JSONFileStore, LakehouseStore, get_store, make_result_key. JSONFileStore CRUD works with real filesystem via tmp_path. LakehouseStore CRUD works with mocked mssparkutils. get_store auto-detection and explicit backend selection work correctly.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_storage.py -x -v` -- all storage tests pass
- `python -c "from dq_framework.storage import ResultStore, JSONFileStore, LakehouseStore, get_store, make_result_key"` -- imports succeed
- `pytest --cov=dq_framework --cov-fail-under=60` -- overall coverage maintained
</verification>

<success_criteria>
- ResultStore ABC exists with write/read/list/delete abstract methods
- JSONFileStore passes all CRUD tests against real filesystem (tmp_path)
- LakehouseStore passes all CRUD tests against mocked mssparkutils
- get_store() correctly auto-selects backend based on runtime
- make_result_key() produces sortable, filesystem-safe keys
- All tests pass, overall coverage >= 60%
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-abstraction/05-01-SUMMARY.md`
</output>
