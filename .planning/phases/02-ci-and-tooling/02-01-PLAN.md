---
phase: 02-ci-and-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - requirements-dev.txt
autonomous: true
requirements:
  - PKG-04
  - PKG-05

must_haves:
  truths:
    - "CI matrix tests Python 3.10, 3.11, 3.12, and 3.13 (not 3.8 or 3.9)"
    - "CI lint job uses ruff check and ruff format --check (no flake8, black, or isort)"
    - "CI test job installs via pip install -e '.[dev]' (no requirements-dev.txt)"
    - "requirements-dev.txt does not exist in the repository"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with ruff linting and 3.10-3.13 matrix"
      contains: "ruff check"
    - path: "requirements-dev.txt"
      provides: "MUST NOT EXIST (deleted)"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "pip install -e '.[dev]' reads [project.optional-dependencies].dev"
      pattern: 'pip install -e "\.\[dev\]"'
---

<objective>
Rewrite the CI workflow to use ruff for linting (replacing flake8/black/isort), align the test matrix to Python 3.10-3.13 (matching pyproject.toml classifiers), switch dependency installation to `pip install -e ".[dev]"`, and delete requirements-dev.txt.

Purpose: CI currently tests unsupported Python versions (3.8, 3.9) and runs three legacy linters that conflict with the ruff config already in pyproject.toml. requirements-dev.txt has conflicting version pins (pytest<8 vs pyproject.toml's pytest>=9).

Output: Updated ci.yml, deleted requirements-dev.txt
</objective>

<execution_context>
@/home/sanmi/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ci-and-tooling/02-RESEARCH.md

<interfaces>
<!-- From pyproject.toml — the source of truth for dependencies and ruff config -->

[project.optional-dependencies]
dev = [
    "pytest>=9.0.0",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.15.0",
    "ruff>=0.15.0",
    "mypy>=1.19.0",
    "pre-commit>=4.0.0",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]

Python classifiers: 3.10, 3.11, 3.12, 3.13
requires-python = ">=3.10"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CI workflow for ruff and Python 3.10-3.13</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Rewrite .github/workflows/ci.yml with the following structure (use the complete target CI from 02-RESEARCH.md as the template):

**lint job:**
- runs-on: ubuntu-latest
- actions/checkout@v4
- actions/setup-python@v5 with python-version: "3.12"
- Install ruff: `pip install ruff`
- Run `ruff check --output-format=github .`
- Run `ruff format --check .`
- Do NOT install flake8, black, or isort. Do NOT run flake8, black --check, or isort --check.

**test job:**
- needs: lint
- Matrix: python-version: ["3.10", "3.11", "3.12", "3.13"] (NOT 3.8 or 3.9)
- fail-fast: false
- Cache pip with key based on pyproject.toml (not requirements*.txt)
- Install deps: `python -m pip install --upgrade pip` then `pip install -e ".[dev]"` (NOT pip install -r requirements.txt or requirements-dev.txt)
- Run: `pytest tests/ -v --cov=dq_framework --cov-report=xml --cov-report=term-missing --cov-fail-under=60`
- Upload coverage on python 3.12 using codecov/codecov-action@v4

**build job:**
- needs: test
- python-version: "3.12"
- pip install build twine, python -m build, twine check dist/*
- Upload artifact with actions/upload-artifact@v4

**security job:**
- needs: lint
- python-version: "3.12"
- Install and run bandit: `bandit -r dq_framework/ -ll -ii`
- Do NOT install safety or run safety check (removed per research recommendation)

Keep the same trigger config: push/PR on master, main, develop.
Remove the `env: PYTHON_DEFAULT_VERSION` block (inline "3.12" directly).
  </action>
  <verify>
    <automated>cd /home/sanmi/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY && python -c "import yaml; y=yaml.safe_load(open('.github/workflows/ci.yml')); m=y['jobs']['test']['strategy']['matrix']['python-version']; assert m==['3.10','3.11','3.12','3.13'], f'Matrix wrong: {m}'; assert 'flake8' not in str(y), 'flake8 found'; assert 'black' not in str(y['jobs']), 'black found'; assert 'isort' not in str(y['jobs']), 'isort found'; print('CI workflow valid')"</automated>
  </verify>
  <done>CI workflow uses ruff for linting, tests Python 3.10-3.13, installs via pip install -e ".[dev]", no references to flake8/black/isort/requirements-dev.txt</done>
</task>

<task type="auto">
  <name>Task 2: Delete requirements-dev.txt</name>
  <files>requirements-dev.txt</files>
  <action>
Delete the file requirements-dev.txt from the repository. This file has conflicting version pins with pyproject.toml (e.g., pytest>=7.4.0,<8.0.0 vs pytest>=9.0.0) and includes redundant tools (black, flake8, isort, pylint, autopep8, sphinx, jupyter, memory-profiler, etc.) that are not needed. All dev dependencies are now managed via pyproject.toml [project.optional-dependencies].dev.

After deletion, verify no other files in the repo reference requirements-dev.txt (the CI workflow was already updated in Task 1). Check for references in README.md, Makefile, or any docs.
  </action>
  <verify>
    <automated>cd /home/sanmi/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY && test ! -f requirements-dev.txt && echo "requirements-dev.txt deleted" && ! grep -r "requirements-dev" .github/ README.md Makefile 2>/dev/null && echo "No remaining references"</automated>
  </verify>
  <done>requirements-dev.txt is deleted. No references to it remain in CI or documentation.</done>
</task>

</tasks>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` -- CI YAML is valid
- CI matrix is exactly ["3.10", "3.11", "3.12", "3.13"]
- No flake8/black/isort references in CI workflow
- requirements-dev.txt does not exist
- `pip install -e ".[dev]"` succeeds (dev dependencies installable from pyproject.toml)
</verification>

<success_criteria>
CI workflow uses ruff for linting, tests Python 3.10-3.13, installs dependencies from pyproject.toml only, and requirements-dev.txt is removed from the repository.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ci-and-tooling/02-01-SUMMARY.md`
</output>
