---
phase: 02-ci-and-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
autonomous: true
requirements:
  - PKG-06

must_haves:
  truths:
    - "Pre-commit hooks use ruff-check and ruff-format (no black, isort, or flake8 hooks)"
    - "ruff-check runs before ruff-format (hook order matters for --fix)"
    - "pre-commit run --all-files completes without configuration errors"
    - "pydocstyle and safety hooks are removed (legacy tools)"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit config with ruff hooks replacing black/isort/flake8"
      contains: "ruff-pre-commit"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "pyproject.toml [tool.ruff]"
      via: "ruff hooks read lint/format config from pyproject.toml"
      pattern: "astral-sh/ruff-pre-commit"
---

<objective>
Replace black, isort, flake8, pydocstyle, and safety hooks in .pre-commit-config.yaml with ruff-check and ruff-format from the official astral-sh/ruff-pre-commit repository. Keep mypy, bandit, and pre-commit-hooks unchanged.

Purpose: The pre-commit config currently runs four separate formatting/linting tools (black, isort, flake8, pydocstyle) that are all replaced by ruff, which is already fully configured in pyproject.toml. Running both creates conflicts and slows down commits.

Output: Updated .pre-commit-config.yaml
</objective>

<execution_context>
@/home/sanmi/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ci-and-tooling/02-RESEARCH.md

<interfaces>
<!-- From pyproject.toml — ruff config that pre-commit hooks will use -->

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["dq_framework"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy linting hooks with ruff in pre-commit config</name>
  <files>.pre-commit-config.yaml</files>
  <action>
Rewrite .pre-commit-config.yaml with the following repos in this exact order:

1. **Ruff (NEW -- replaces black, isort, flake8):**
```yaml
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.15.5
  hooks:
    - id: ruff-check
      args: [--fix]
    - id: ruff-format
```
CRITICAL: ruff-check MUST come before ruff-format (--fix can produce code that needs formatting).

2. **Mypy (KEEP as-is):**
```yaml
- repo: https://github.com/pre-commit/mirrors-mypy
  rev: v1.8.0
  hooks:
    - id: mypy
      additional_dependencies: [types-pyyaml, pandas-stubs]
      args: ['--ignore-missing-imports', '--strict-optional']
```

3. **Bandit (KEEP as-is):**
```yaml
- repo: https://github.com/PyCQA/bandit
  rev: 1.7.6
  hooks:
    - id: bandit
      args: ['-r', 'dq_framework/', '-ll']
```

4. **General file checks (KEEP as-is):**
```yaml
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v4.5.0
  hooks:
    - id: check-yaml
      args: ['--safe']
    - id: check-json
    - id: check-toml
    - id: check-added-large-files
      args: ['--maxkb=1000']
    - id: check-merge-conflict
    - id: check-case-conflict
    - id: detect-private-key
    - id: end-of-file-fixer
    - id: trailing-whitespace
    - id: mixed-line-ending
```

**REMOVE entirely (do NOT include):**
- `repo: https://github.com/psf/black` (replaced by ruff-format)
- `repo: https://github.com/PyCQA/isort` (replaced by ruff-check with I rules)
- `repo: https://github.com/PyCQA/flake8` (replaced by ruff-check)
- `repo: https://github.com/pycqa/pydocstyle` (not needed; can add D rules to ruff later)
- `repo: https://github.com/Lucas-C/pre-commit-hooks-safety` (references requirements*.txt; safety has gone freemium)

Keep a brief header comment (2-3 lines) explaining installation (`pip install pre-commit && pre-commit install`). Do not include the long original header block.
  </action>
  <verify>
    <automated>cd /home/sanmi/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY && python -c "
import yaml
with open('.pre-commit-config.yaml') as f:
    cfg = yaml.safe_load(f)
repos = [r['repo'] for r in cfg['repos']]
assert 'https://github.com/astral-sh/ruff-pre-commit' in repos, 'ruff-pre-commit missing'
assert 'https://github.com/psf/black' not in repos, 'black still present'
assert 'https://github.com/PyCQA/isort' not in repos, 'isort still present'
assert 'https://github.com/PyCQA/flake8' not in repos, 'flake8 still present'
assert 'https://github.com/pycqa/pydocstyle' not in repos, 'pydocstyle still present'
# Check ruff-check comes before ruff-format
ruff_repo = [r for r in cfg['repos'] if 'ruff-pre-commit' in r['repo']][0]
hook_ids = [h['id'] for h in ruff_repo['hooks']]
assert hook_ids == ['ruff-check', 'ruff-format'], f'Hook order wrong: {hook_ids}'
print('Pre-commit config valid')
"</automated>
  </verify>
  <done>Pre-commit config uses ruff-check and ruff-format (in correct order) from astral-sh/ruff-pre-commit. Black, isort, flake8, pydocstyle, and safety hooks are removed. Mypy, bandit, and pre-commit-hooks are preserved.</done>
</task>

</tasks>

<verification>
- `.pre-commit-config.yaml` is valid YAML
- Contains astral-sh/ruff-pre-commit with ruff-check before ruff-format
- No references to black, isort, flake8, pydocstyle, or safety hooks
- Mypy, bandit, and pre-commit-hooks retained
- `pre-commit run --all-files` runs without hook configuration errors (may report lint issues in code, which is expected and not a blocker for this phase)
</verification>

<success_criteria>
Pre-commit hooks use ruff for all linting and formatting, with legacy tool hooks completely removed. Hook order is correct (ruff-check before ruff-format).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ci-and-tooling/02-02-SUMMARY.md`
</output>
