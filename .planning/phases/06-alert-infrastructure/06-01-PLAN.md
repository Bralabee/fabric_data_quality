---
phase: 06-alert-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dq_framework/alerting/__init__.py
  - dq_framework/alerting/formatter.py
  - dq_framework/alerting/config.py
  - dq_framework/alerting/templates/summary.txt.j2
  - dq_framework/alerting/templates/summary.html.j2
  - tests/test_alerting/__init__.py
  - tests/test_alerting/test_formatter.py
  - tests/test_alerting/test_config.py
  - pyproject.toml
autonomous: true
requirements: [ALRT-03, ALRT-05]

must_haves:
  truths:
    - "Jinja2 templates render alert messages containing validation summary, failed expectations, and severity breakdown"
    - "AlertConfig.from_dict() parses an alerts: YAML section into typed dataclasses with sensible defaults"
    - "Missing or empty alerts: section produces a disabled AlertConfig (enabled=False)"
    - "Environment variable substitution works in YAML string values via ${VAR_NAME} syntax"
  artifacts:
    - path: "dq_framework/alerting/__init__.py"
      provides: "Public API exports for alerting subpackage"
      exports: ["AlertFormatter", "AlertConfig", "ChannelConfig", "CircuitBreakerConfig", "FailurePolicy"]
    - path: "dq_framework/alerting/formatter.py"
      provides: "Jinja2 template rendering for alert messages"
      exports: ["AlertFormatter"]
      min_lines: 30
    - path: "dq_framework/alerting/config.py"
      provides: "YAML alert config parsing into dataclasses"
      exports: ["AlertConfig", "ChannelConfig", "CircuitBreakerConfig", "FailurePolicy"]
      min_lines: 50
    - path: "dq_framework/alerting/templates/summary.txt.j2"
      provides: "Default plain-text alert template"
      contains: "DATA QUALITY ALERT"
    - path: "dq_framework/alerting/templates/summary.html.j2"
      provides: "Default HTML alert template for email channel (Phase 7)"
      contains: "<html"
    - path: "tests/test_alerting/test_formatter.py"
      provides: "Unit tests for AlertFormatter"
      min_lines: 40
    - path: "tests/test_alerting/test_config.py"
      provides: "Unit tests for AlertConfig parsing"
      min_lines: 40
  key_links:
    - from: "dq_framework/alerting/formatter.py"
      to: "dq_framework/alerting/templates/"
      via: "Jinja2 PackageLoader"
      pattern: "PackageLoader.*dq_framework\\.alerting"
    - from: "dq_framework/alerting/config.py"
      to: "os.environ"
      via: "env var substitution in YAML values"
      pattern: "os\\.environ"
---

<objective>
Create the alerting subpackage with Jinja2-based message formatting and YAML-driven alert configuration parsing.

Purpose: Establish the data structures and formatting layer that the dispatcher (Plan 02) and channel implementations (Phase 7) will consume. This is the "contracts" plan -- defining AlertConfig, ChannelConfig, FailurePolicy, and the template rendering interface.

Output: `dq_framework/alerting/` subpackage with formatter, config, default templates, and full test coverage.
</objective>

<execution_context>
@/home/sanmi/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-alert-infrastructure/06-RESEARCH.md

<!-- Reference for ABC/subpackage pattern established in Phase 5 -->
@dq_framework/storage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create alerting subpackage with AlertFormatter and Jinja2 templates</name>
  <files>
    dq_framework/alerting/__init__.py,
    dq_framework/alerting/formatter.py,
    dq_framework/alerting/templates/summary.txt.j2,
    dq_framework/alerting/templates/summary.html.j2,
    tests/test_alerting/__init__.py,
    tests/test_alerting/test_formatter.py,
    pyproject.toml
  </files>
  <behavior>
    - Test: AlertFormatter with default templates (PackageLoader) renders plain-text containing suite_name, success status, success_rate, failed_checks count
    - Test: AlertFormatter with custom template_dir (FileSystemLoader) renders from user-supplied templates
    - Test: render("summary.txt.j2", results) includes failed expectations with severity and column name
    - Test: render("summary.txt.j2", results) includes severity breakdown section when severity_stats present
    - Test: render("summary.txt.j2", results) includes threshold violations when threshold_failures present
    - Test: render("summary.txt.j2", results) omits empty sections (no "Failed Expectations:" header when list is empty)
    - Test: render("summary.html.j2", results) produces valid HTML with table structure
    - Test: AlertFormatter raises TemplateNotFound for nonexistent template name
  </behavior>
  <action>
    1. Create `dq_framework/alerting/` directory with `__init__.py` (empty initially, exports added after implementation).
    2. Create `tests/test_alerting/` directory with `__init__.py`.
    3. Write `tests/test_alerting/test_formatter.py` with pytest-native test class `TestAlertFormatter` implementing all behaviors above. Use a `sample_results` fixture returning a dict matching the validation result structure from `validator.py` (keys: suite_name, batch_name, success, success_rate, evaluated_checks, failed_checks, failed_expectations, severity_stats, threshold_failures, timestamp). Tests should initially fail (RED).
    4. Create `dq_framework/alerting/templates/summary.txt.j2` using the template from 06-RESEARCH.md Code Examples section.
    5. Create `dq_framework/alerting/templates/summary.html.j2` with an HTML version: `<html>` wrapper, `<table>` for summary stats, `<ul>` for failed expectations, inline CSS for email rendering.
    6. Implement `dq_framework/alerting/formatter.py` with `AlertFormatter` class:
       - `__init__(self, template_dir: str | None = None)` -- uses PackageLoader("dq_framework.alerting", "templates") by default, FileSystemLoader(template_dir) if provided. Set `autoescape=select_autoescape(["html"])`, `trim_blocks=True`, `lstrip_blocks=True`.
       - `render(self, template_name: str, results: dict) -> str` -- renders template with result keys extracted per research pattern.
    7. Update `pyproject.toml` to add `[tool.setuptools.package-data]` section: `dq_framework = ["alerting/templates/*.j2"]` to ensure templates are included in package builds.
    8. Update `dq_framework/alerting/__init__.py` to export `AlertFormatter`.
    9. Run tests (GREEN).

    Note: Use `jinja2` which is already available via great-expectations transitive dependency. Do NOT add jinja2 to pyproject.toml dependencies (it would be redundant).
  </action>
  <verify>
    <automated>cd /mnt/Storage/Insync/olusanmi_18th@hotmail.com/OneDrive/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY && python -m pytest tests/test_alerting/test_formatter.py -x --no-cov -q</automated>
  </verify>
  <done>
    AlertFormatter renders plain-text and HTML templates with validation data. PackageLoader finds templates from the alerting subpackage. Custom template_dir works via FileSystemLoader. All 8+ tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AlertConfig with YAML parsing, FailurePolicy enum, and env var substitution</name>
  <files>
    dq_framework/alerting/config.py,
    dq_framework/alerting/__init__.py,
    tests/test_alerting/test_config.py
  </files>
  <behavior>
    - Test: AlertConfig.from_dict(None) returns AlertConfig(enabled=False)
    - Test: AlertConfig.from_dict({}) returns AlertConfig(enabled=False)
    - Test: AlertConfig.from_dict(full_config) parses enabled, failure_policy, channels list, circuit_breaker settings
    - Test: ChannelConfig parses type, enabled flag, and extra settings dict
    - Test: CircuitBreakerConfig has defaults (failure_threshold=5, cooldown_seconds=300.0)
    - Test: AlertConfig.from_dict with missing circuit_breaker key uses defaults
    - Test: FailurePolicy enum has WARN, RAISE, FALLBACK members
    - Test: AlertConfig.from_dict with invalid failure_policy raises ValueError
    - Test: Environment variable substitution replaces ${VAR_NAME} in string values when env var exists
    - Test: Environment variable substitution leaves ${VAR_NAME} unchanged when env var is missing (logs warning)
    - Test: Non-string values (int, bool, list) are not affected by env var substitution
  </behavior>
  <action>
    1. Write `tests/test_alerting/test_config.py` with pytest-native test class `TestAlertConfig` implementing all behaviors above. Use a `full_alert_config` fixture with the YAML structure from 06-RESEARCH.md Pattern 4. For env var tests, use `monkeypatch.setenv`/`monkeypatch.delenv`. Tests should initially fail (RED).
    2. Implement `dq_framework/alerting/config.py`:
       - `FailurePolicy(Enum)` with WARN, RAISE, FALLBACK members (string values: "warn", "raise", "fallback").
       - `AlertDeliveryError(Exception)` for RAISE policy failures.
       - `ChannelConfig` dataclass with `type: str`, `enabled: bool = True`, `settings: dict = field(default_factory=dict)`.
       - `CircuitBreakerConfig` dataclass with `failure_threshold: int = 5`, `cooldown_seconds: float = 300.0`.
       - `AlertConfig` dataclass with `enabled: bool = True`, `failure_policy: FailurePolicy = FailurePolicy.WARN`, `channels: list[ChannelConfig]`, `circuit_breaker: CircuitBreakerConfig`, `templates: dict[str, str]`.
       - `AlertConfig.from_dict(cls, data: dict | None) -> AlertConfig` classmethod:
         - Returns `AlertConfig(enabled=False)` if data is None or empty.
         - Calls `_resolve_env_vars(data)` to substitute `${VAR_NAME}` patterns in string values using `os.environ.get()`.
         - Parses failure_policy string to FailurePolicy enum (raises ValueError for invalid values).
         - Parses channels list into ChannelConfig objects (channel-specific keys beyond type/enabled go into settings dict).
         - Parses circuit_breaker dict into CircuitBreakerConfig (uses defaults if missing).
         - Parses templates dict (pass-through).
       - `_resolve_env_vars(data: dict) -> dict` module-level function: recursively walks dict, replaces `${VAR_NAME}` in string values using `re.sub` with `os.environ.get(var_name, match.group(0))`. Logs warning for unresolved vars.
    3. Update `dq_framework/alerting/__init__.py` to export: `AlertFormatter`, `AlertConfig`, `ChannelConfig`, `CircuitBreakerConfig`, `FailurePolicy`, `AlertDeliveryError`.
    4. Run tests (GREEN).
  </action>
  <verify>
    <automated>cd /mnt/Storage/Insync/olusanmi_18th@hotmail.com/OneDrive/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY && python -m pytest tests/test_alerting/test_config.py -x --no-cov -q</automated>
  </verify>
  <done>
    AlertConfig.from_dict() correctly parses full YAML alert config into typed dataclasses. FailurePolicy enum validates policy strings. Env var substitution works with ${VAR_NAME} syntax. All 11+ tests pass.
  </done>
</task>

</tasks>

<verification>
Run the full alerting test suite to confirm both tasks work together:
```bash
cd /mnt/Storage/Insync/olusanmi_18th@hotmail.com/OneDrive/Documents/HS2/HS2_PROJECTS_2025/2_DATA_QUALITY_LIBRARY
python -m pytest tests/test_alerting/ -x --no-cov -q
```

Verify package data config:
```bash
python -c "from dq_framework.alerting import AlertFormatter, AlertConfig, ChannelConfig, CircuitBreakerConfig, FailurePolicy, AlertDeliveryError; print('All exports OK')"
```

Verify templates are loadable:
```bash
python -c "from dq_framework.alerting import AlertFormatter; f = AlertFormatter(); print(f.render('summary.txt.j2', {'suite_name': 'test', 'success': True, 'success_rate': 100, 'batch_name': 'b', 'evaluated_checks': 5, 'failed_checks': 0, 'failed_expectations': [], 'severity_stats': {}, 'threshold_failures': [], 'timestamp': '2026-03-08'}))"
```
</verification>

<success_criteria>
1. `dq_framework/alerting/` subpackage exists with formatter.py, config.py, __init__.py, and templates/
2. AlertFormatter renders both .txt.j2 and .html.j2 templates with validation result data
3. AlertConfig.from_dict() parses YAML alerts: section into typed dataclasses
4. FailurePolicy enum has WARN/RAISE/FALLBACK members
5. Environment variable substitution works in config string values
6. pyproject.toml includes package-data for .j2 template files
7. All tests pass: `pytest tests/test_alerting/ -x --no-cov`
</success_criteria>

<output>
After completion, create `.planning/phases/06-alert-infrastructure/06-01-SUMMARY.md`
</output>
